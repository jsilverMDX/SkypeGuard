INSTALL

For it to work, Skype must be running.
First run will require it to be authorized as an API client in the Skype app.


Ruby script:

Install Ruby (official version, 2.2+)

Install "skype" and "active_support" gem

Run with "ruby skypeguard.rb"

Whitelist handles by editing @whitelist array



RubyMotion app:

Install RubyMotion (not starter edition unfortunately... sorry)

"bundle install"

"rake"